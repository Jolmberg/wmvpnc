Scroll display when password exceeds 9 characters

Make a bigger fuss when the vpn goes down unexpectedly

Event-driven main loop (requires changes to wmdocklib)

Allow disconnect button to abort a connection at any time (while typing the password for example)
